# Env vars referenced in app/ or src/ that are runtime-injected (by Node.js,
# npm/pnpm, or the build tooling) rather than user-configurable — these don't
# belong in .env.local.example. See scripts/check-env-example.mjs.

NODE_ENV
npm_package_version
